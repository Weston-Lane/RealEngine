#D:\Projects\RealEngine\RealEngine
cmake_minimum_required(VERSION 3.14)
# RealEngine as shared library
file(GLOB_RECURSE REALENGINE_SRC CONFIGURE_DEPENDS
    "${CMAKE_CURRENT_SOURCE_DIR}/src/*.cpp"
    "${CMAKE_CURRENT_SOURCE_DIR}/src/*.h"
)

add_library(RealEngine SHARED ${REALENGINE_SRC})

target_include_directories(RealEngine PUBLIC
    ${CMAKE_CURRENT_SOURCE_DIR}/src
    ${CMAKE_CURRENT_SOURCE_DIR}/vendor
)

#target_compile_definitions(RealEngine PRIVATE REALENGINE_EXPORTS)
# Optionally add third-party dependencies here
# target_link_libraries(RealEngine ...)
